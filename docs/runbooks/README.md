# Runbooks

Operational playbooks land here as the system grows. Stubs to fill in:

- `edgebox-not-syncing.md` — outbox grows but nothing reaches cloud.
- `camera-offline.md` — RTSP reconnect loop, evidence to capture.
- `sensor-flatline.md` — MQTT broker disconnects, tracing to source.
- `model-rollback.md` — switching `latest` symlink back to a prior version.
- `disk-full.md` — pruning outbox, snapshots, training artifacts.
