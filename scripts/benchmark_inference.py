"""Benchmark inference latency on the current host.

Lands in Sprint 2 once we have a real model wired in. Skeleton kept here so the
location is settled.
"""

from __future__ import annotations


def main() -> int:
    raise NotImplementedError("Implement once YOLO adapter is real (Sprint 2).")


if __name__ == "__main__":
    raise SystemExit(main())
